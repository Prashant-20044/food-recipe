{
  "name": "tastenest",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend/food-app",
    "dev": "npm run dev --prefix backend",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend/food-app"
  }
}
